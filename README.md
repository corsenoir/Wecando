
  # We Can Do

  This is a code bundle for We Can Do. The original project is available at https://www.figma.com/design/LftxMO77oubnEEQqIEawXr/We-Can-Do.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  